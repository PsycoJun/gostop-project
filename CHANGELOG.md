# 변경사항 기록 (Changelog)

이 프로젝트의 모든 주요 변경사항은 이 파일에 문서화됩니다.

## [2.1] - 2025-09-21

### 변경 (Changed)
- '게임시작' 버튼 UI를 '나가기' 버튼과 동일하게 수정 (배경색 제외). 패딩, 마진, 폰트 크기, 폰트 굵기, 테두리, 그림자, 전환 효과 등을 일치시킴.

### 삭제 (Removed)
- 사용되지 않는 `client/src/app/page.module.css` 파일 삭제.

## [1.9] - 2025-09-21

### 수정 (Fixed)
- `page.tsx`의 게임 보드 UI가 올바르게 표시되지 않던 문제를 수정했습니다.

### 변경 (Changed)
- '게임시작' 및 '준비완료' 버튼의 크기를 통일하고, 활성화/비활성화 상태에 따른 색상 구분을 명확히 했습니다.

## [1.8] - 2025-09-21

### 변경 (Changed)
- '게임시작' 버튼의 활성화/비활성화 상태가 시각적으로 명확히 구분되도록 스타일을 변경했습니다.
  - **활성화**: 토스 블루 (`#0064FF`)
  - **비활성화**: 어두운 색 + 반투명 처리

## [1.7] - 2025-09-21

### 추가 (Added)
- 변경사항이 올바르게 적용되었는지 최종 확인하기 위해, `page.tsx`에 `v1.7 Code Running!` 콘솔 로그를 추가했습니다.

## [1.6] - 2025-09-21

### 리팩토링 (Refactoring)
- `page.tsx`의 방장 확인 로직을 간결하게 리팩토링했습니다.
- 디버깅 과정에서 사용되었던 불필요한 변수(`me`, `isHostFromPlayerList`)를 제거하고, 이제 정상적으로 동작하는 `isHost` 변수를 사용하도록 코드를 정리했습니다.

## [1.5] - 2025-09-21

### 수정 (Fixed)
- `auth.tsx`에서 JWT 토큰 디코딩 시 `userId`를 `sub` 클레임에서 찾던 것을 실제 필드명인 `userId`에서 찾도록 수정하여, 방장이 자신을 인식하지 못하던 치명적인 버그를 최종적으로 해결했습니다.

## [1.4] - 2025-09-21

### 추가 (Added)
- JWT 토큰의 실제 내용을 확인하기 위해 `auth.tsx`에 디버깅 로그를 추가했습니다.

## [1.3] - 2025-09-21

### 수정 (Fixed)
- `useAuth` 훅이 `userId`를 포함하도록 `auth.tsx`의 `user` 상태와 타입 정의를 수정했습니다.

## [1.2] - 2025-09-21

### 수정 (Fixed)
- 방장 이름 옆에 왕관(👑) 아이콘이 다시 표시되도록 복구했습니다.

### 추가 (Added)
- '게임시작' 버튼 버그의 원인 진단을 위해 `page.tsx`에 ID 비교 디버깅 로그를 추가했습니다.

## [1.1] - 2025-09-21

### 변경 (Changed)
- 방 제목을 "Room <ID>" 형식에서 "<방장 이름>의 방"으로 변경하여 사용자 경험을 개선했습니다.

### 추가 (Added)
- 방 제목 옆에 버전 번호를 표시하는 기능을 추가했습니다.
